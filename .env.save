# ── Midnights01 — Network Tools Server ────────────────────────────────────────
# Copy this file to .env and fill in the values
# NEVER commit .env to Git

TZ=America/Chicago

# ── NetBox ────────────────────────────────────
NETBOX_SUPERUSER=admin
NETBOX_SUPERUSER_EMAIL=bvaughters.soc@gmail.com
NETBOX_SUPERUSER_PASSWORD=
NETBOX_API_TOKEN=
# Generate with: openssl rand -hex 20
NETBOX_SECRET_KEY=
# Generate with: openssl rand -hex 32
NETBOX_DB_PASSWORD=

# ── Oxidized ──────────────────────────────────
# No env vars needed — configured via config files

# ── Smokeping ─────────────────────────────────
# No env vars needed — configured via config files

# ── Uptime Kuma ───────────────────────────────
# No env vars needed — configured via web UI
